# photogrammetry-project
this program, takes a dataset, asks the user for the kind of model it wants, gets the number of ground control points &amp; check points. the calculates the parameters for that model, the residuals, the RMSE, &amp; finally, plots the derived coordinates using the desired model &amp; the gcp coordinates &amp; shows the residuals visually.
